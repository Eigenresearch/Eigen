eigen 1.0

let half_pi: float = PI / 2
let quarter_pi: float = PI / 4

qubit q0

# Redundant self-inverse gates: should be canceled
H q0
H q0

X q0
X q0

# Consecutive rotations on same qubit: should be merged into RX q0, PI/2
RX q0, quarter_pi
RX q0, quarter_pi
